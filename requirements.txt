# python 3.12 or 3.13, linux x86_64. this is the whole benchmark except the
# two optional backends, which are gigabytes and live in requirements-backends.txt
# onnx2tf pins this stack (numpy, onnx, onnxruntime, onnxscript, litert, tf),
# so everything below follows its pins
torch==2.11.0
numpy==2.2.6
onnx==1.20.1
onnxscript==0.6.2
onnxruntime==1.26.0

librosa==1.0.0
soundfile
# the mfcc dct comes straight from scipy, not through librosa
scipy
matplotlib
pytest
